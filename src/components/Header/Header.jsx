import { AppBar, Toolbar, Typography } from "@mui/material";

export default function Headers() {
  return (
    <AppBar position="static">
      <Toolbar>
        <Typography variant="h6">Typing test</Typography>
      </Toolbar>
    </AppBar>
  );
}
